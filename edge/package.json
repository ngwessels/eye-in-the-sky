{
  "name": "@eye/edge",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "prebuild": "npm run build -w @eye/shared",
    "build": "tsc",
    "start": "node start.mjs",
    "test": "node --import tsx/esm --test src/omni-camera-count.test.ts"
  },
  "dependencies": {
    "@eye/shared": "*",
    "dotenv": "^16.4.7",
    "sharp": "^0.34.5"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
